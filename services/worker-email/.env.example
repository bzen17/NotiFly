MONGO_URI=REPLACE_WITH_MONGO_URI
PG_CONNECTION=REPLACE_WITH_PG_CONNECTION
REDIS_URL=REPLACE_WITH_REDIS_URL

# App
STREAM=notifications.email
GROUP=email-workers

# When true, adapter uses mock implementations instead of real providers
USE_MOCK=false

# SendGrid configuration - set your real key when sending real emails
# If you have a key, set it here; otherwise leave blank for mock mode.
SENDGRID_API_KEY=REPLACE_WITH_SENDGRID_API_KEY
SENDGRID_FROM=REPLACE_WITH_SENDGRID_FROM_EMAIL