# Example env variables for docker-compose (copy to .env and fill secrets)
# Copy this file to `.env` and DO NOT commit `.env` to version control.

# Postgres
POSTGRES_USER=postgres
POSTGRES_PASSWORD=change_me
POSTGRES_DB=notifly

# MongoDB
MONGO_INITDB_ROOT_USERNAME=root
MONGO_INITDB_ROOT_PASSWORD=change_me

# Optional: specify other env vars used by services (no defaults set)
# e.g. REDIS_PASSWORD=...
